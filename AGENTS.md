
# BESMA 운영 규칙 (besma.co.kr)

## 1. 최상위 규칙 보관소
- JSI 최상위 규칙 파일은 `D:\JSI\AGENTS.md`를 사용한다.
- `AGENTS.md`와 충돌하면 `AGENTS.md`의 지침을 우선한다.

## 2. 문서 업로드/다운로드 운영 원칙
- 기존 업로드 기록은 삭제/덮어쓰기 대상이 아니다. 기존 행은 보존하고, 날짜·근로자·문서종류 기준으로 적절히 upsert한다.
- 업로드 실패 처리나 덮어쓰기 전에는 반드시 작업 전 스냅샷(쿼리 결과 요약 또는 DB dump/CSV)을 남긴다.
- 업로드 실패 시 즉시 롤백 가능한 점검 상태로 되돌린다.

## 3. 문서 라우트 보전
- `/documents`, `/document-submissions`, `/hq-safe/documents`, `/site/documents` 경로는 유지한다.
- `/hq-safe/document-instances/:id`, `/documents/hq-dashboard`, `/documents/requirements/status` 같은 핵심 API 경로는 이름과 의미를 변경하지 않는다.

## 4. 문서 상태 해석 금지 변경
- 문서 상태값은 기존 의미 유지: `NOT_REQUIRED`, `NOT_SUBMITTED`, `SUBMITTED`, `IN_REVIEW`, `APPROVED`, `REJECTED`.
- `REWORK_REQUIRED`는 응답 스키마에서 사용하지 않는다.

## 5. 본사 조회전용 규칙
- `HQ_OTHER`/본사 조회전용에서는 임의 수정, 승인 처리, 엑셀/PDF 다운로드를 허용하지 않는다.
- 조회 화면은 조회/열람 위주로만 동작해야 한다.

## 6. 기능인인정제(안전평가) 핵심 불변
- 한 번 평가 완료된 근로자는 향후 날짜/재출입과 무관하게 평가 완료 상태를 유지해야 한다.
- 6/16 이후 시작 시점 기준으로 일자를 기준으로 “오늘 안 나왔거나 재미방문”한 경우에도 과거 평가결과를 지우지 않는다.
- `미반영`은 아직 평가되지 않은 근로자만 의미해야 한다.
- 평가완료 데이터 계산 API는 실제 출석 대상(worker attendance) + 기존 평가 이력(worker any assessment) 합집합 기반으로 계산한다.

## 7. 매일 엑셀 재반영 방어
- 매일 엑셀을 재업로드해도 HQ 요약/진척/완료 카운트는 기존 완료 이력을 깎아먹으면 안 된다.
- 기간이 겹치는 데이터(중첩 출입)로 인해 완료자 목록은 그대로 유지되고, 미완료자 목록만 변해야 한다.

## 8. 평가/문서 건수 변경 시 검증 순서
- 변경 전/후로 최소한 다음을 저장한다: 대상 기간, 대상 근로자 수, 평가완료 수, 미반영 수.
- 변경 후에는 즉시 다음으로 검증:
  - `api.besma.co.kr/health` 상태 정상
  - 프론트 alias(`www.besma.co.kr`) 최신 배포 확인
  - `git rev-parse HEAD` + `git ls-remote`로 반영 커밋 일치

## 9. 배포 규칙
- 배포 순서: backend 반영 후 frontend 반영(필요 시 각각의 스크립트/파이프라인 사용).
- 배포만 하고 확인 미완료 상태에서 “반영 완료”라고 공지하지 않는다.
- 배포/작업 로그는 누락 없이 보관한다.

## 10. 변경 시나리오 안전 조항
- 동작 변경은 기능 단위로 나누고, 하나의 큰 일괄 수정 후 추측으로 판단하지 않는다.
- `본사-현장`, `문서취합`, `HQ_SAFE`, `SITE` 경로/상태/권한은 각기 독립적으로 회귀 테스트 범위를 나눠 점검한다.
- 의문점이 생기면 우선 소거: 임시 규칙을 도입하지 않고 원복 가능한 상태에서 원인 로그부터 확보한다.

## 11. 재발사례 추적 (필수 반영 규칙)
- 위험신호: 기능인인정제 스크립트/코드 수정 후 기존 완료자 PDF/동의서가 일괄 삭제되는 이력 검증:
  - `7310a3d chore: 기능인제 동의서·평가보고서 PDF 파일 삭제 스크립트` (삭제 스크립트의 기본 동작이 즉시 삭제)
- 대응 조치:
  - 6/15~6/19 사이 실패 분석에서 확인된 포인트를 반영해, 동의/보고서 PDF 삭제는 기본값을 `dry-run`로 두고 `--apply --confirm <TOKEN>`이 있어야 실행되도록 강제.
  - `reset` 성격 스크립트는 반드시 `--dry-run` 또는 `--apply` 둘 중 하나만 허용하고, `--apply`는 토큰 확인 후에만 진행.

## 12. 파일/DB 영속성 보전 하드코딩 금지
- DB upsert 경로를 작성할 때 기존 행을 덮어쓰거나 삭제하지 않고, 기존 평가 완료자의 `worker any assessment` 이력을 보존하는 로직을 default 처리로 유지한다.
- 새 파일 업로드/재반영이 `평가완료` 상태를 소거하면 즉시 롤백한다.
- 같은 근로자라도 10년 뒤 재방문해도 기존 완료 기록은 유지한다.

## 13. 변경 실행 전 증거 남기기
- 스크립트 실행 전/후에는 대상 수치(`평가완료 수`, `미반영 수`, `DB row counts`)를 출력/기록한다.
- 1) `git show <commit>`으로 삭제성능 수정이 있는지 확인, 2) `git diff`에 delete 쿼리/`delete()` 체인 존재 여부 점검, 3) 배포 직후 재집계 API 응답 비교를 수행한다.

## 14. 자동 복구형 개발 루틴
- 작업 단위는 `checkpoint commit`을 기본으로 운영한다. 세션 또는 기능 블록 종료 시:
  1) `git add -A`
  2) `git commit -m "checkpoint: <요약>"`  (필요 시 `--no-verify`)
  3) 영향 범위가 확정되면 `git tag checkpoint/<YYYYMMDD>-<HHMMSS>` 를 동시에 생성
- 가능하면 `checkpoint`는 자동 push 전제값으로 두고, 운영 push는 사용자 승인 `git push`로 분리한다.
- 롤백 방법:
  - 이전 상태로 되돌림: `git revert --no-edit <commit>` (공유된 이력 보존)
  - 즉시 복구(로컬만): `git reset --hard <커밋>`
  - 체크포인트 태그로 즉시 이동: `git checkout checkpoint/<태그>` 또는 `git switch -c hotfix/<태그> checkpoint/<태그>`

## 15. 로그인 병목/세션 재발방지
- /auth/login 실패/성공은 반드시 운영 로그에 남아야 하며, 실패 이벤트는 최소 7일 보존하여 IP·로그인ID·경과시간 단위로 집계한다.
- 동일 IP에서 동일 login_id 실패 5회/분 이상이면 알림 또는 응답 제한(지수백오프) 기준을 운영 정책에 맞춰 적용한다.
- 장애 대응 시 로그인 문제 원인 분석은 nginx access, backend journal, app 로그를 시간순으로 묶어 10분 내 보고서를 작성한다.
- 세션 의존 기능(자동 갱신/권한 변경)이 있으면 토큰 반납 시점 재시도를 허용하는 정책(재로그인 대기시간 상한, 화면 재진입 메시지)을 문서화한다.

## 16. 모델 선택 운영 기준
- 기본 분석·설계·운영 장애 대응·배포 판단은 GPT 5.5 기준으로 수행한다.
- 단순 git 작업(`status`, `add`, `commit`, `tag`, 사용자 승인 후 `push`)이나 이미 결정된 짧은 이행 작업은 Spark 사용을 우선한다.
- Spark 사용 대상은 코드 구조 판단이 거의 필요 없고, 되돌리기 쉬우며, 결과가 명확한 작업으로 제한한다.
- 인증/세션/DB 삭제·초기화/운영 배포/평가완료 데이터 보존처럼 사고 영향이 큰 작업은 단순해 보여도 GPT 5.5로 검토한다.
- 모델 전환 시에는 작업 로그에 사용 모델과 이유를 간단히 남긴다.

